#include <stdio.h>

int main()
{
    int num, row;

    for (num = 1; num <= 5; num++)
    {
        for (row = 1; row <= 3; row++)
        {
            printf("*");
        printf("\n");
        }
    }

    return 0;
}